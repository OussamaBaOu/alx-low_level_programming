#include "main.h"
/**
 * _atoi - computes the absolute value of an integer
 * @r: the int to check
 * Return: the absolute value of int
 */
int _atoi(char *s)
{
	if (s >= 0)
	{
		return (*s);
	}
	return (s);
}
