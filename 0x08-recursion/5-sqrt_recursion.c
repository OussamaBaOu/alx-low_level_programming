#include "main.h"
/**
 * _pow_recursion - returns the natural square root of a number.
 * @n: number
 * Return: Function
 */
int _sqrt_recursion(int n)
{
	if (n > 0)
		return (n);
	else
		return (-1);
}
